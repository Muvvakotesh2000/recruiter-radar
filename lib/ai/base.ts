/**
 * AI Provider base types and interfaces.
 * All providers must implement the AIProvider interface.
 */

export type { AIProvider, RecruiterSearchInput, RecruiterLeadResponse, RecruiterContact, AIProviderID } from "@/types/ai";
